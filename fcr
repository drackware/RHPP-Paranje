```html
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kalkulator FCR Harian</title>

    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background: #f3f4f6;
            color: #111827;
        }

        .container {
            max-width: 500px;
            margin: 30px auto;
            padding: 15px;
        }

        .card {
            background: white;
            border-radius: 16px;
            padding: 25px;
            box-shadow: 0 5px 20px rgba(0,0,0,.08);
        }

        h1 {
            text-align: center;
            margin-top: 0;
            font-size: 24px;
        }

        .subtitle {
            text-align: center;
            color: #6b7280;
            margin-bottom: 25px;
        }

        label {
            display: block;
            font-weight: bold;
            margin-bottom: 7px;
        }

        .form-group {
            margin-bottom: 18px;
        }

        input {
            width: 100%;
            padding: 13px;
            border: 1px solid #d1d5db;
            border-radius: 10px;
            font-size: 16px;
            outline: none;
        }

        input:focus {
            border-color: #2563eb;
        }

        .unit {
            font-size: 13px;
            color: #6b7280;
            margin-top: 5px;
        }

        button {
            width: 100%;
            padding: 14px;
            border: none;
            border-radius: 10px;
            background: #2563eb;
            color: white;
            font-size: 17px;
            font-weight: bold;
            cursor: pointer;
        }

        button:hover {
            background: #1d4ed8;
        }

        .result {
            margin-top: 25px;
            padding: 20px;
            background: #f8fafc;
            border-radius: 12px;
            display: none;
        }

        .result h2 {
            margin-top: 0;
            text-align: center;
            font-size: 20px;
        }

        .row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #e5e7eb;
        }

        .row:last-child {
            border-bottom: none;
        }

        .value {
            font-weight: bold;
        }

        .fcr {
            margin-top: 15px;
            padding: 18px;
            background: #2563eb;
            color: white;
            border-radius: 12px;
            text-align: center;
        }

        .fcr-label {
            font-size: 14px;
            opacity: .9;
        }

        .fcr-value {
            font-size: 38px;
            font-weight: bold;
            margin-top: 5px;
        }

        .error {
            color: #dc2626;
            text-align: center;
            margin-top: 15px;
            display: none;
        }

        .formula {
            margin-top: 20px;
            padding: 12px;
            background: #f3f4f6;
            border-radius: 8px;
            font-size: 13px;
            color: #4b5563;
            text-align: center;
        }
    </style>
</head>

<body>

<div class="container">

    <div class="card">

        <h1>Kalkulator FCR Harian</h1>
        <div class="subtitle">
            Perhitungan FCR berdasarkan rataan bobot ayam
        </div>

        <div class="form-group">
            <label>Jumlah Ayam</label>
            <input type="number" id="populasi" placeholder="Contoh: 10000" min="1">
            <div class="unit">Ekor</div>
        </div>

        <div class="form-group">
            <label>Rataan Bobot Awal</label>
            <input type="number" id="bobotAwal" placeholder="Contoh: 1000" min="0">
            <div class="unit">Gram / ekor</div>
        </div>

        <div class="form-group">
            <label>Rataan Bobot Akhir</label>
            <input type="number" id="bobotAkhir" placeholder="Contoh: 1050" min="0">
            <div class="unit">Gram / ekor</div>
        </div>

        <div class="form-group">
            <label>Pakan Terpakai</label>
            <input type="number" id="sak" placeholder="Contoh: 20" min="0" step="0.01">
            <div class="unit">Sak (1 sak = 50 kg)</div>
        </div>

        <button onclick="hitungFCR()">HITUNG FCR</button>

        <div class="error" id="error"></div>

        <div class="result" id="result">

            <h2>Hasil Perhitungan</h2>

            <div class="row">
                <span>Pakan terpakai</span>
                <span class="value" id="hasilPakan">0 kg</span>
            </div>

            <div class="row">
                <span>Kenaikan bobot / ekor</span>
                <span class="value" id="hasilKenaikan">0 gram</span>
            </div>

            <div class="row">
                <span>Total kenaikan bobot</span>
                <span class="value" id="hasilTotalBobot">0 kg</span>
            </div>

            <div class="fcr">
                <div class="fcr-label">FCR HARIAN</div>
                <div class="fcr-value" id="hasilFCR">0.00</div>
            </div>

            <div class="formula">
                FCR = Total Pakan ÷ Total Kenaikan Bobot
            </div>

        </div>

    </div>

</div>

<script>

function hitungFCR() {

    const populasi = parseFloat(document.getElementById("populasi").value);
    const bobotAwal = parseFloat(document.getElementById("bobotAwal").value);
    const bobotAkhir = parseFloat(document.getElementById("bobotAkhir").value);
    const sak = parseFloat(document.getElementById("sak").value);

    const error = document.getElementById("error");
    const result = document.getElementById("result");

    error.style.display = "none";
    result.style.display = "none";

    // Validasi
    if (
        isNaN(populasi) ||
        isNaN(bobotAwal) ||
        isNaN(bobotAkhir) ||
        isNaN(sak)
    ) {
        error.innerText = "Silakan isi semua data.";
        error.style.display = "block";
        return;
    }

    if (populasi <= 0) {
        error.innerText = "Jumlah ayam harus lebih dari 0.";
        error.style.display = "block";
        return;
    }

    if (bobotAkhir <= bobotAwal) {
        error.innerText = "Bobot akhir harus lebih besar dari bobot awal.";
        error.style.display = "block";
        return;
    }

    if (sak <= 0) {
        error.innerText = "Jumlah pakan harus lebih dari 0.";
        error.style.display = "block";
        return;
    }

    // 1 sak = 50 kg
    const totalPakan = sak * 50;

    // Kenaikan bobot per ekor dalam gram
    const kenaikanGram = bobotAkhir - bobotAwal;

    // Total kenaikan bobot:
    // gram -> kg
    const totalKenaikanKg =
        (kenaikanGram * populasi) / 1000;

    // FCR
    const fcr = totalPakan / totalKenaikanKg;

    // Tampilkan hasil
    document.getElementById("hasilPakan").innerText =
        totalPakan.toFixed(2) + " kg";

    document.getElementById("hasilKenaikan").innerText =
        kenaikanGram.toFixed(0) + " gram";

    document.getElementById("hasilTotalBobot").innerText =
        totalKenaikanKg.toFixed(2) + " kg";

    document.getElementById("hasilFCR").innerText =
        fcr.toFixed(2);

    result.style.display = "block";
}

</script>

</body>
</html>
```
